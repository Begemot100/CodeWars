def find_it(seq):
    my_dict = {i:seq.count(i) for i in seq}
    print(my_dict)
    lst = my_dict.values()
    for i in lst:
        if i%2 != 0:
            return (list(my_dict.keys())[list(my_dict.values()).index(i)])
